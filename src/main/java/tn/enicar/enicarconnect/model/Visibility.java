package tn.enicar.enicarconnect.model;

public enum Visibility {
    PUBLIC, // Visible par tous les utilisateurs
    GROUP, // Visible par des groupes spécifiques
    PRIVATE // Visible uniquement par l'auteur
}
