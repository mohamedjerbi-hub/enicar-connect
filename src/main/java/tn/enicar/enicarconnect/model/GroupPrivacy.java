package tn.enicar.enicarconnect.model;

public enum GroupPrivacy {
    PUBLIC, // Visible et ouvert à tous
    PRIVATE, // Visible mais adhésion soumise à validation
    INVITE_ONLY // Invisible, uniquement accessible sur invitation
}
